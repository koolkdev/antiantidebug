# This file cleans some garbage
# In the vm code, there are groups of 
# PUSH_DX
# changing dx (all the groups of garbage)
# POP_DX

# Those are the instructions that are using to do the garbage, and don't use dx
DEFINE_GROUP NON_DX_RELATED STACK_PUSH_REGREF STACK_ADD PUSH_DWORD_IMM
DEFINE_GROUP[32] NON_DX_RELATED PUSH_WORD_IMM
DEFINE_GROUP[64] NON_DX_RELATED PUSH_QWORD_IMM

DEFINE_GROUP MATH_OPS_FLAGS ADD SUB XOR AND OR BT BTC BTR BTS IMULTWO ADC SBB CMP TEST INC DEC RCR RCL ROL ROR SAR SHL SHR MUL IMUL DIV IDIV NEG
DEFINE_GROUP SIMPLE_MATH ADD SUB XOR AND OR
DEFINE_GROUP NON_DX_RELATED ^SIMPLE_MATH|F_DWORD STACK_POP_DWORD_REG
DEFINE_GROUP CHANGE_DX STACK_POP_DX STACK_MOV_DX_SP

DEFINE_GROUP SIZES BYTE WORD DWORD
DEFINE_GROUP[64] SIZES QWORD

DEFINE_MACRO[32] SIZE_WORD DWORD
DEFINE_MACRO[32] SIZE_NUM 0x4
DEFINE_MACRO[64] SIZE_WORD QWORD
DEFINE_MACRO[64] SIZE_NUM 0x8


DEFINE_TEMPLATE_MACRO UNRELATED_POP_DX
*^NON_DX_RELATED
&I=^CHANGE_DX

# After we will do the cleaning of what between those two, we will stay with the PUSH_DX POP_DX, so we will jsut remove them 
DEFINE_TEMPLATE
STACK_PUSH_DX
STACK_POP_DX
=>

DEFINE_TEMPLATE[32]
PUSH_WORD_IMM X
PUSH_WORD_IMM Y
STACK_POP_DX
@UNRELATED_POP_DX
=>
NOP
NOP
NOP
*I*

DEFINE_TEMPLATE[64]
PUSH_DWORD_IMM X
STACK_POP_DX
@UNRELATED_POP_DX
=>
NOP
NOP
*I*

DEFINE_TEMPLATE
STACK_PUSH_DX
PUSH_@SIZE_WORD@_IMM @SIZE_NUM@
STACK_ADD
STACK_POP_DX
@UNRELATED_POP_DX
=>
NOP
NOP
NOP
NOP
*I*

DEFINE_TEMPLATE
STACK_PUSH_REGREF X
STACK_POP_DX
STACK_PUSH_@SIZE_WORD@_MEMDX
STACK_POP_DX
@UNRELATED_POP_DX
=>
NOP
NOP
NOP
NOP
*I*

DEFINE_TEMPLATE
STACK_PUSH_REGREF
STACK_POP_DX
@UNRELATED_POP_DX
=>
NOP
NOP
*I*

# Dummy opeartions to change the flags before the real operaion
# Note: It seems that they have bug sometimes and use dx after destroying it
DEFINE_TEMPLATE
STACK_PUSH_REGREF X
STACK_POP_DX
STACK_PUSH_@SIZE_WORD@_MEMDX
STACK_PUSH_REGREF Y
STACK_POP_DX
STACK_PUSH_@SIZE_WORD@_MEMDX
^SIMPLE_MATH|F_DWORD
STACK_POP_DWORD_REG 0x7
STACK_POP_DX
&OP=^MATH_OPS_FLAGS|F_|&SIZE=^SIZES
=>
NOP
NOP
NOP
NOP
NOP
NOP
NOP
NOP
NOP
*OP*F_*SIZE*

DEFINE_TEMPLATE
NOP
=>


